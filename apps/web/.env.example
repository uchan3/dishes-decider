# PWA が Supabase に接続するための公開値（クライアントに露出してよい。anon key は RLS で防御）。
# .env.local にコピーして実際の値を入れる（.env.local は git 管理外）。
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
